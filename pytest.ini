# pytest.ini (at project root)
[pytest]
minversion = 7.0
# Make discovery explicit and stable:
testpaths =
    tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: lightweight integration checks (run in CI/scheduler)
