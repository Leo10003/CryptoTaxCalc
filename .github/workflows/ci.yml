name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  secret-scan:
    name: Secret scan (gitleaks)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dependency-audit:
    name: Dependency vulnerability audit (pip-audit)
    runs-on: ubuntu-latest
    needs: secret-scan
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pip-audit
        run: |
          python -m pip install --upgrade pip pip-audit

      - name: Audit dependencies (OSV)
        shell: bash
        run: |
          set -euo pipefail
          if [ -f requirements.txt ]; then
            # requirements.txt may include "-e ."; exclude editables for audit accuracy.
            grep -vE '^\s*(-e|--editable)\s' requirements.txt > /tmp/requirements_audit.txt
            pip-audit -r /tmp/requirements_audit.txt
          else
            pip-audit
          fi   

  lint-and-tests:
    name: Lint & Tests (Linux, py${{ matrix.python-version }})
    needs: secret-scan
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('pyproject.toml', 'requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-

      - name: Install project (editable) + dev tools
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          # Optional: validate requirements.txt only when explicitly requested
          if [ "${CI_INSTALL_REQUIREMENTS:-0}" = "1" ] && [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
        env:
          PIP_DISABLE_PIP_VERSION_CHECK: "1"

      - name: Lint / format checks
        run: |
          if [ -f .pre-commit-config.yaml ]; then
            pre-commit run --all-files
          else
            ruff check .
            black --check .
          fi

      - name: Run tests (pytest)
        env:
          PYTHONPATH: "${{ github.workspace }}/src"
        run: |
          pytest -q -m "not smoke"

  ps-lint:  
    name: PowerShell lint (Windows)
    needs: secret-scan
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install PowerShell 7 (pwsh) & PSScriptAnalyzer
        shell: pwsh
        run: |
          # windows-latest already has pwsh; ensure analyzer is present
          Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser -AllowClobber
          Import-Module PSScriptAnalyzer

      - name: Lint automation scripts
        shell: pwsh
        run: |
          $paths = @(
            "automation/collect_support_bundle.ps1"
          ) | Where-Object { Test-Path $_ }
          if ($paths.Count -gt 0) {
            Invoke-ScriptAnalyzer -Path $paths -Recurse -Severity @('Error','Warning') -ReportSummary
          } else {
            "No PowerShell files found to lint." | Write-Host
          }
